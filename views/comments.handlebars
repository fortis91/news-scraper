{{!-- <div class="container">
    <div class="text-left mb-3">
        <h2>News Scraper</h2>
    </div>
    <div class="row">
        <div class="col-12">
            Commenting on:
            <div class="card mt-1 mb-2">
                <div class="card-horizontal">
                    <div class="card-body">
                        <p>{{ dbArticle.title }}</p>
                        <br>
                        <p>{{ dbArticle.summary }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            {{#each dbArticle.notes}}
            <div class="card mt-1 mb-2" style="_width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">{{ title }}</h5>
                    <p class="card-text">{{ body }}</p>
                    <button type="button" class="btn btn-primary js-deleteComment" data-id={{ _id }}>Delete</button>
                </div>
            </div>
            {{/ each}}
        </div>
        <div class="col-6">
            <form class="mb-3">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" class="form-control" id="title">
                </div>
                <div class="form-group">
                    <label for="body">Comment</label>
                    <input type="textarea" class="form-control" id="body">
                </div>
                <button type="submit" class="btn btn-primary" id="submitButton">Add Comment</button>
                <a href="/" class="btn btn-primary">Back to Scraping</button>
            </form>
        </div>

    </div>
</div> --}}

<div class="container">
    {{!-- {{#each dbArticles}} --}}
    <div class="row">
        <div class="col-12">
            <div class="col-6">
                {{#each dbArticle.notes}}
                <div class="card mt-1 mb-2" style="_width: 18rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{ title }}</h5>
                        <p class="card-text">{{ body }}</p>
                        <button type="button" class="btn btn-primary js-deleteComment" data-id={{ _id }}>Delete</button>
                    </div>
                </div>
                {{/ each}}
            </div>
            <div class="col-6">
                <form class="mb-3">
                    <div class="form-group">
                        <label for="title">Title</label>
                        <input type="text" class="form-control" id="title">
                    </div>
                    <div class="form-group">
                        <label for="body">Comment</label>
                        <input type="textarea" class="form-control" id="body">
                    </div>
                    <button type="submit" class="btn btn-primary" id="submitButton">Add Comment</button>
                    <a href="/" class="btn btn-primary">Back to Scraping</button>
                </form>
            </div>
            {{!-- <div class="card mt-1 mb-2">
                <div class="card-horizontal">
                    <div class="card-body">
                        <p class="card-title">{{title}}</p>
                        <br>
                        <p>{{summary}}</p>
                        <a href={{link}} target="_blank" class="btn btn-primary float-sm-right btn-sm">Read</a>
                        <a href="/comments/{{_id}}" class="btn btn-primary float-sm-right btn-sm">Comments</a>
                    </div>
                </div>
            </div> --}}
        </div>
    </div>
    {{!-- {{/each}} --}}
</div>
<script type="text/javascript" src="/assets/js/news.js"></script>